<form method="post" action="{{ path('app_member_delete', {'id': member.id}) }}" onsubmit="return confirm('La suppression est définitive, confirmer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ member.id) }}">
    <button class="margin-10  font-size-20">Supprimer</button>
</form>
