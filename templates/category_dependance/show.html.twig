{% extends 'base.html.twig' %}

{% block title %}CategoryDependance{% endblock %}

{% block body %}
    <h1>CategoryDependance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_dependance.id }}</td>
                <th>item</th>
                <td>{{ category_dependance.item }}</td>
                <th>item</th>
                <td>{{ category_dependance.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_dependance_index') }}">back to list</a>

    <a href="{{ path('app_category_dependance_edit', {'id': category_dependance.id}) }}">edit</a>

    {{ include('category_dependance/_delete_form.html.twig') }}
{% endblock %}
